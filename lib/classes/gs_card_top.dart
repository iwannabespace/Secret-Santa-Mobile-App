import 'package:flutter/material.dart';

class GiftScreenCardTop {
  GiftScreenCardTop(
      {required this.title, required this.subTitle, required this.color});

  String title;
  String subTitle;
  Color color;
}
